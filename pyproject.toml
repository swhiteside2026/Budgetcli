[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "budgetcli"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
budget = "budgetcli.cli:main"
